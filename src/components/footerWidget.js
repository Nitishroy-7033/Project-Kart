import { Row } from "antd";

const FooterWidget = () => {
  return (
    <Row
      style={{
        height: "400px",
        backgroundColor: "var(--lable-color)",
      }}
    ></Row>
  );


};

export default FooterWidget;
