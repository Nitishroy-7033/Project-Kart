import { Button, Empty, Row, Typography } from "antd";
const NotFoundPage = () => {
  return (
    <Row
      justify="center"
      align="middle"
      style={{
        height: "100vh",
      }}
    >
   This is not found page
    </Row>
  );
};

export default NotFoundPage;
